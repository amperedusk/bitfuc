# Volunteer bitfuc-test peers

**TEST COINS — NO VALUE.**

This list is a phone book, not the ledger. Nodes do not need it to validate.
If it goes stale, use `addnode` with an address you trust.

Format: `host:27333` plus who runs it (optional).

| Address | Notes |
| --- | --- |
| *(none yet)* | Add a row if you keep a node reachable |

Do not list RPC ports. Public-chain peers belong in `docs/mainnet-peers.md`.
